name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using ssh key
      uses: appleboy/ssh-action@master
      with:
        host: us2-gcp.tloxygen.com
        username: ze3kr
        key: ${{ secrets.KEY }}
        fingerprint: SHA256:V0QNBz2Gz4XcqNfrTxbPmmSyKDqDNNbLc9LTqCg9M4k
        script: |-
          /usr/bin/git -C /home/ze3kr/sync/ZE3kr.com pull
